<?xml version="1.0" encoding="utf-8"?>

<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/base_notif_button_layout"
    android:background="?attr/actionBarItemBackground"
    android:minHeight="@dimen/abc_action_button_min_height_material"
    android:minWidth="@dimen/abc_action_button_min_width_material"
    android:layout_width="wrap_content" android:layout_height="wrap_content"
    android:clickable="true">

    <ImageView
        android:layout_height="wrap_content"
        android:layout_width="wrap_content"
        android:layout_centerInParent="true"
        android:src="@drawable/ic_action_view_as_grid"/>

    <LinearLayout
        android:id="@+id/total_notification_indicator"
        android:minWidth="@dimen/abc_action_button_min_width_material"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:visibility="invisible"
        android:gravity="right|end">

        <TextView
            android:id="@+id/total_notification_indicator_text"
            android:background="@drawable/red_circle"
            android:layout_width="20dp"
            android:layout_height="20dp"
            android:layout_marginRight="4dp"
            android:layout_marginEnd="4dp"
            android:layout_marginTop="2dp"
            android:textSize="12sp"
            android:gravity="center"
            android:shadowRadius="2"
            android:shadowDx="1"
            android:shadowDy="1"
            android:shadowColor="@color/black"
            android:textStyle="bold"
            android:textColor="@color/white"/>

    </LinearLayout>

</RelativeLayout>